export enum customerStatus {
  P = "P",
  C = "C",
}
export enum custstatus {
  G = "G",
  P = "P",
  R = "R",
  S = "S",
}

export enum taskName {
  PERSONAL = "Personal",
  SERVICE = "Service",
  SUBSCRIPTION = "Subscription",
  INVESTMENT = "Investment",
  DOCUMENT = "Document",
  DEMAT = "Demat",
}

export enum requestAdvice {
  ALLOWED = "allowed",
  NOTALLOWED = "notallowed",
}

export enum ReqInvtype {
  NO_TAX_SAVING = "Non-Tax saving",
  TAX_SAVING = "Tax saving",
}

export enum MessageStatus {
  NEW = "NEW",
  READ = "READ",
}

export enum ServiceType {
  ADVISORY = "Advisory",
  TRAINING = "Training",
}
